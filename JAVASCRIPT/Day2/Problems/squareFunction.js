// Function to calculate the square of a number
function square(x) {
    // Multiply the number by itself
    let result = x * x;

    // send the result back
    return result;
}

// Call the function with input value 5
let value = square(5);

// Print the result to the console
console.log("Square of 5 is:", value);
